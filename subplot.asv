% helper function to identify the data type
% true if 'TLM', elif: 'Areas', else: skip
function [] = subplot(m, n, title, figlure_label, legend_options, x_fit_range, labels);
     h = plot( fitresult, xData, yData, figlure_label );
     title(char(title));
     axis([xmin-0.1*abs(xmin) xmax+0.1*abs(xmax) -1 1])
     axis 'auto y'
     legend( h, legend, 'Zero Line', 'Location', 'SouthEast' );
     % Label axes
     xlabel= labels(1);
     ylabel= labels(2);
     grid on